# 关卡入口 UI 冻结说明 - 2026-05-27

## 冻结结论

关卡入口 UI 界面已调整完成并确认固定保存。

后续默认不得再修改关卡入口界面相关布局、尺寸、坐标、素材引用和交互表现，除非用户明确提出“解冻关卡入口 UI”或明确要求继续调整该界面。

## 冻结范围

- 顶栏：玩家级数牌、猫咪金币、设置按钮的位置、尺寸和数值显示。
- 中央：游戏标题、竖向关卡滚动栏、关卡图、`START/NEXT/lock-crop`、通关印章位置。
- 背景：`home-bg.png`、`home-bg-alt.png` 的入口界面使用方式。
- 底栏：主页、店铺、罐头、手账入口图标的位置、尺寸和透明 PNG 展示方式。
- 入口页缓存版本：当前以 `20260527-entry-ui-layout-21` 作为确认版。

## 后续开发约束

- 新增玩法、关卡、结算、手账、店铺、罐头等功能时，不应顺手改动关卡入口 UI。
- 若必须改动共享 CSS，应先确认不会影响 `.home-view`、`.home-phone`、`.home-status`、`.level-scroll-panel`、`.home-bottom-nav` 等入口页样式。
- 若用户未来要求替换素材，应只替换同名资源文件，优先保持现有布局坐标不变。
