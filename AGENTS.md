# AGENTS.md

本文件面向后续参与本项目的开发 AI。开始任何修改前，请先阅读本说明，并遵守当前工作区的组织规则。

## 目录用途

- `docs/`
  - 产品文档区。
  - 存放 PRD、需求迭代记录、关键决策、会议结论、验收标准等项目文档。
  - 任何需求变更或重要技术/产品决策，都应在此目录中留下记录。

- `assets/design/`
  - 设计素材区。
  - 存放效果图、UI 参考、设计稿导出图、交互参考截图等。

- `assets/bug/`
  - 测试报错截图区。
  - 存放缺陷复现截图、控制台报错截图、异常状态截图等。

- `assets/reference/`
  - 参考资料区。
  - 存放灵感图、竞品截图、视觉参考、外部实现参考等非正式素材。

- `notes/`
  - 学习笔记区。
  - 存放开发过程中踩过的坑、调研结论、技术方案比较、临时学习记录等。

- `src/`
  - 独立代码区。
  - 所有项目代码都应放在此目录下，包括前端、后端、脚本、配置模板和测试代码。

## 开发规则

0. 遵循产品先行的协作流程
   - 不要在想法模糊时直接写代码。
   - 新功能或新玩法应先在 `docs/` 中整理为 PRD，并与用户讨论确认。
   - PRD 确认后，优先制作最小可玩 Demo，用于验证玩法方向、操作手感和视觉表达。
   - Demo 方向确认后，再补全功能逻辑，并在 `docs/` 中制定分步骤开发计划。
   - 开发与测试过程中遇到的 bug、技术坑和方案结论，应记录到 `notes/`。

1. 代码集中存放在 `src/`
   - 不要在根目录或文档目录中散落业务代码。
   - 如需新增模块、页面、组件、工具函数或测试，请优先在 `src/` 内建立清晰的子目录。

2. 代码区不依赖外部工作区文件
   - `src/` 内代码不应直接引用 `docs/`、`notes/` 或 `assets/reference/` 中的文件作为运行时依赖。
   - 若某个资源是应用运行所必需的，应复制或移动到 `src/` 内合适的资源目录，并在文档中说明来源。

3. 文档统一放入 `docs/`
   - PRD、需求说明、迭代记录、接口约定、验收标准和关键决策都放入 `docs/`。
   - 根目录只保留必要的入口文档，例如 `README.md` 和本文件。

4. 素材按用途归档
   - UI 效果图和设计参考放入 `assets/design/`。
   - 缺陷、报错、测试截图放入 `assets/bug/`。
   - 灵感、竞品和外部参考放入 `assets/reference/`。

5. 学习和排障记录写入 `notes/`
   - 遇到可复用的技术结论、调试方法或踩坑记录，应写入 `notes/`。
   - 笔记可以保持轻量，但标题应清晰，便于后续检索。

6. 修改前先理解上下文
   - 开发前先查看相关 `docs/` 文档和已有 `src/` 结构。
   - 不要做无关重构，不要移动无关文件。
   - 若已有用户修改，必须保留并基于现状继续工作。

7. 保持项目可运行
   - 新增或修改代码后，应尽量运行对应测试、构建或格式检查。
   - 若无法验证，需要在最终说明中明确原因和风险。

8. 命名建议
   - 文档文件名建议使用清晰中文或英文短语，例如 `PRD.md`、`iteration-log.md`、`tech-decisions.md`。
   - 截图和素材建议包含日期或场景，例如 `2026-05-25-login-error.png`。

## 推荐初始结构

```text
.
├── AGENTS.md
├── README.md
├── docs/
├── assets/
│   ├── design/
│   ├── bug/
│   └── reference/
├── notes/
└── src/
```
